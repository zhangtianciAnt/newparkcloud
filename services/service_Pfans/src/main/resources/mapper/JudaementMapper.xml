<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nt.service_pfans.PFANS1000.mapper.JudgementMapper">
    <select id="selectJudgement"  resultType="com.nt.dao_Pfans.PFANS1000.Judgement">
select JUDGEMENT_ID as judgementid,
       judgnumbers,
       USER_ID,
       CENTER_ID,
       GROUP_ID,
       TEAM_ID,
       INVESTIGATOR,
       FILENAME,
       SCHEDULEDDATE,
       CAREERPLAN,
       BUSINESSPLANTYPE,
       CLASSIFICATIONTYPE,
       BUSINESSPLANBALANCE,
       AMOUNTTOBEGIVEN,
       GIST,
       PURCHASSUPPORT,
       NUMBERS,
       UNITPRICE,
       MONEY,
       THISPROJECT,
       SETTINGPLACE,
       ADDBOOK,
       SALEQUOTATION,
       REASONSFORQUOTATION,
       UPLOADFILE,
       FREEDEVICE,
       EQUIPMENT,
       DECISIVE,
       PERIOD,
       STARTDATE,
       ENDDATE,
       TYPE,
       CREATEBY,
       CREATEON,
       MODIFYBY,
       MODIFYON,
       OWNER,
       STATUS,
       TENANTID,
       ROWINDEX
       from judgement where `STATUS` = '4' ORDER BY CREATEON DESC
    </select>

       <select id="getJudgementSearch" parameterType="com.nt.dao_Pfans.PFANS1000.Judgement" resultType="com.nt.dao_Pfans.PFANS1000.Judgement">
              SELECT
              *
              FROM
              judgement
              where status != '1'
                     <if test="equipment != null and equipment != ''">
                            and equipment = #{equipment}
                     </if>
                     <if test="judgnumbers != null and judgnumbers != ''">
                            and judgnumbers like CONCAT('%',#{judgnumbers},'%')
                     </if>
                     <if test="user_id != null and user_id != ''">
                            and user_id = #{user_id}
                     </if>
                     <if test="center_id != null and center_id != ''">
                            and center_id = #{center_id}
                     </if>
                     <if test="group_id != null and group_id != ''">
                            and group_id = #{group_id}
                     </if>
                     <if test="owners != null and owners.size() > 0 ">
                            and
                            owner in
                            <foreach item="item" index="index" collection="owners"
                                     open="(" separator="," close=")">
                                   #{item}
                            </foreach>
                     </if>
       </select>

</mapper>

