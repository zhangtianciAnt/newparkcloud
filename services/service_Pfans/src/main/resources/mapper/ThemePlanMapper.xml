<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nt.service_pfans.PFANS1000.mapper.ThemePlanMapper">
<!--	<select id="getThemePlanList" resultType="com.nt.dao_Pfans.PFANS1000.ThemePlan">-->
<!--		select tp.*-->
<!--		from themeplan tp-->
<!--		where not exists (-->
<!--		select * from themeplan tpn-->
<!--		where tpn.MONTHS= tp.MONTHS-->
<!--		and tpn.status != tp.status-->
<!--		and tpn.TYPE = #{type}-->
<!--		AND tpn.YEARS = #{years}-->
<!--		AND tpn.MONTHS IS NOT NULL-->
<!--		)-->
<!--		AND tp.MONTHS IS NOT NULL-->
<!--		and tp.type = #{type}-->
<!--		order by  tp.CREATEON desc-->
<!--	</select>-->
</mapper>
