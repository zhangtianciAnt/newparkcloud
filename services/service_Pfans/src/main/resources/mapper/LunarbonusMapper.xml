<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nt.service_pfans.PFANS2000.mapper.LunarbonusMapper">
    <!--    根据权限显示一览-->
    <select id="selectSee" resultType="com.nt.dao_Pfans.PFANS2000.Lunarbonus">
        SELECT LUNARBONUS_ID, GROUP_ID, EVALUATIONDAY, SUBJECTMON, SUBJECT, EVALUATENUM, USER_ID, CREATEBY, CREATEON, MODIFYBY, MODIFYON, OWNER, STATUS, TENANTID
        FROM lunarbonus
        WHERE  1 = 1
        <if test="lunarbonusList != null and lunarbonusList.size() > 0 ">
            AND EVALUATENUM IN
            <foreach item="item" index="index" collection="lunarbonusList"
                     open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
        ORDER BY EVALUATIONDAY DESC, SUBJECTMON DESC, EVALUATENUM DESC
    </select>
</mapper>
