<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nt.service_pfans.PFANS6000.mapper.DelegainformationMapper">
    <select id="getDelegainformation" resultType="com.nt.dao_Pfans.PFANS6000.Vo.DelegainformationVo">
         SELECT
	a.*,
	b.SUPPLIERNAMEID,
	b.COMPANY,
	c.PROJECT_NAME,
	c.MANAGERID,
	d.OPERATIONFORM,
	d.JOBCLASSIFICATION,
	d.ALLTECHNOLOGY,
	d.SITEVALUATION,
	d.EXITREASON,
	d.BUSINESSIMPACT,
	d.COUNTERMEASURE
FROM
	delegainformation a
	INNER JOIN projectsystem b ON a.PROJECTSYSTEM_ID = b.PROJECTSYSTEM_ID
	INNER JOIN companyprojects c ON a.COMPANYPROJECTS_ID = c.COMPANYPROJECTS_ID
	INNER JOIN expatriatesinfor d ON b.SUPPLIERNAMEID = d.SUPPLIERNAMEID
    </select>
</mapper>

