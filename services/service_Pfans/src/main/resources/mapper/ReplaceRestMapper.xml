<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nt.service_pfans.PFANS2000.mapper.ReplacerestMapper">
    <update id="updateDateList">
        <![CDATA[
        update replacerest
        set recognitionstate = '2', status = '1'
        WHERE type = '2'
        AND recognitionstate = '0'
        AND state = '0'
        AND str_to_date(APPLICATION_DATE, '%Y-%m-%d') < curdate()
        AND str_to_date(APPLICATION_DATE, '%Y-%m-%d') >= date_sub(curdate(), interval 1 YEAR)
        ]]>
    </update>
</mapper>
