<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nt.service_pfans.PFANS1000.mapper.NapalmMapper">
    <select id="selectList" parameterType="com.nt.dao_Pfans.PFANS1000.Napalm" resultType="com.nt.dao_Pfans.PFANS1000.Napalm">
        SELECT
            *
        FROM
            NAPALM
        WHERE
            STATUS != '1'
        <if test="deliveryfinshdate != null">
            AND DELIVERYFINSHDATE LIKE CONCAT( '%', ( date_format(#{deliveryfinshdate},'%Y-%m') ), '%' )
        </if>
        <if test="owners != null and owners.size() > 0 ">
            AND OWNER in
            <foreach item="item" index="index" collection="owners" open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
    </select>
</mapper>
