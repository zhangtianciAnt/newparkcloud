<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nt.service_BASF.mapper.FireaccidentrecordMapper">

    <!--    消防事故记录列表-->
    <select id="selectList" resultType="com.nt.dao_BASF.VO.FireaListVo">
       SELECT
        fireaccidentrecord.*
        ,firealarm.indevice
        ,firealarm.accunit
        ,firealarm.CREATEON AS firealarmcreateon
        FROM fireaccidentrecord
        LEFT JOIN
        firealarm
        ON fireaccidentrecord.firealarmno = firealarm.firealarmno
        WHERE
        fireaccidentrecord.`STATUS`='0'
    </select>

</mapper>