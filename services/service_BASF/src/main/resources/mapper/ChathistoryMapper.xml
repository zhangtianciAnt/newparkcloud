<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nt.service_BASF.mapper.ChathistoryMapper">
    <resultMap id="BaseResultMap" type="com.nt.dao_BASF.Chathistory">
        <id column="id" property="id" jdbcType="VARCHAR"/>
        <result column="from" property="from" jdbcType="VARCHAR"/>
        <result column="to" property="to" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="VARCHAR"/>
        <result column="type" property="type" jdbcType="VARCHAR"/>
        <result column="time" property="time" jdbcType="TIMESTAMP"/>
        <result column="STATUS" property="status" jdbcType="VARCHAR"/>
        <result column="CREATEON" property="createon" jdbcType="TIMESTAMP"/>
        <result column="CREATEBY" property="createby" jdbcType="VARCHAR"/>
        <result column="MODIFYON" property="modifyon" jdbcType="TIMESTAMP"/>
        <result column="MODIFYBY" property="modifyby" jdbcType="VARCHAR"/>
        <result column="Owner" property="owner" jdbcType="VARCHAR"/>
        <result column="TENANTID" property="tenantid" jdbcType="VARCHAR"/>
    </resultMap>

    <insert id="insertChathistoryList" parameterType="java.util.List">
        insert into chathistory
        (
        id,
        from,
        to,
        content,
        type,
        time,
        `status`,
        createon,
        createby,
        modifyon,
        modifyby,
        `owner`,
        tenantid
        )
        values
        <foreach collection="list" item="pd" index="index" separator=",">
            (
            #{pd.id},
            #{pd.from},
            #{pd.to},
            #{pd.content},
            #{pd.type},
            #{pd.time},
            #{pd.status},
            #{pd.createon},
            #{pd.createby},
            #{pd.modifyon},
            #{pd.modifyby},
            #{pd.owner},
            #{pd.tenantid}
            )
        </foreach>

    </insert>
</mapper>
