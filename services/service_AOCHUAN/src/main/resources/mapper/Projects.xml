<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nt.service_AOCHUAN.AOCHUAN3000.mapper.ProjectsMapper">

    <!--  存在Check  -->
    <select id="existCheck" resultType="com.nt.dao_AOCHUAN.AOCHUAN3000.Projects">
        SELECT * FROM projects WHERE PROJECTS_ID = #{id} AND  STATUS = #{status}
    </select>

    <!--  唯一性Check  -->
    <select id="uniqueCheck" resultType="com.nt.dao_AOCHUAN.AOCHUAN3000.Projects">
        SELECT * FROM projects 	WHERE PROJECTS_ID != #{id} AND PRODUCT_NM = #{product_nm} AND PROVIDER = #{provider}
    </select>

    <!--  获取项目表数据  -->
    <select id = "getProjectList" resultType="com.nt.dao_AOCHUAN.AOCHUAN3000.Projects">
        SELECT * FROM projects WHERE STATUS = '0'
    </select>

    <!--  项目表删除  -->
    <update id="deleteFromProjectsByDoubleKey">
        UPDATE projects
		SET STATUS = #{status},
		MODIFYBY = #{modifyby},
		MODIFYON = now()
		 WHERE PRODUCT_NM = #{product_nm}
        AND PROVIDER = #{provider}
    </update>

</mapper>
