<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nt.service_AOCHUAN.AOCHUAN3000.mapper.ProjectsMapper">

    <!--  存在Check  -->
    <select id="existCheck" resultType="com.nt.dao_AOCHUAN.AOCHUAN3000.Projects">
        SELECT * FROM projects WHERE PROJECTS_ID = #{id} AND  STATUS != '1'
    </select>

    <!--  唯一性Check  -->
    <select id="uniqueCheck" resultType="com.nt.dao_AOCHUAN.AOCHUAN3000.Projects">
        SELECT * FROM projects 	WHERE PROJECTS_ID != #{id} AND PRODUCTS_ID = #{products_id} AND SUPPLIER_ID = #{supplier_id} AND STATUS != '1'
    </select>

    <!--  获取项目表数据  -->
    <select id = "getProjectList" resultType="com.nt.dao_AOCHUAN.AOCHUAN3000.Projects">
        SELECT * FROM projects WHERE STATUS != '1' and
        1=1
        <if test="owners != null and owners.size() > 0 ">
            and
            USER_ID in
            <foreach item="item" index="index" collection="owners"
                     open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
    </select>

    <!--  项目表删除  -->
    <update id="deleteFromProjectsByDoubleKey">
        UPDATE projects
		SET STATUS = #{status},
		MODIFYBY = #{modifyby},
		MODIFYON = now()
		 WHERE PRODUCTS_ID = #{products_id}
        AND SUPPLIER_ID = #{supplier_id}
    </update>


    <select id="getForSupplier" resultType="com.nt.dao_AOCHUAN.AOCHUAN3000.Projects">
        select
        projects_id,
        product_nm,
        supplier
        from projects
        where SUPPLIER_ID = #{id}
    </select>

    <select id="getForCustomer" resultType="com.nt.dao_AOCHUAN.AOCHUAN3000.Projects">
        select
        pro.projects_id,
        pro.product_nm,
        pro.supplier
        from projects as pro
        inner join followuprecord as foll
        on pro.products_id = foll.products_id
        where CUSTOMER_ID = #{id}
    </select>
</mapper>
