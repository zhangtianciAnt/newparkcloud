<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nt.service_AOCHUAN.AOCHUAN3000.mapper.ProjectsMapper">
    <!--  获取记录表数据  -->
    <select id="getFollowUpRecordList" resultType="com.nt.dao_AOCHUAN.AOCHUAN3000.FollowUpRecord">
        select
        SAL_CUSTOMER,SAL_DATE,SAL_EVENT,SAL_CONTENT,
        PROC_DATE,PROC_ANSWER
        from followuprecord as fur
        inner join projects as pro
        on fur.PRODUCT_NM = pro.PRODUCT_NM and fur.PROVIDER = pro.PROVIDER
        where fur.PRODUCT_NM =#{product_nm} AND fur.PROVIDER =#{provider}
        order by fur.SAL_DATE
	</select>

    <delete id="deleteByDoubleKey">
        DELETE FROM #{table_nm}
        WHERE product_nm = #{product_nm}
        AND provider = #{provider}
    </delete>

</mapper>
