<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nt.service_AOCHUAN.AOCHUAN7000.mapper.EntryRulesMapper">

    <update id="delCreruleid">
        UPDATE crerule
		SET STATUS = '1',
		MODIFYBY = #{modifyby},
		MODIFYON = now()
		WHERE DOCURULE_FID = #{docurule_fid_id}
    </update>

    <!--  Check  -->
    <select id="existCheckAcc" resultType="int">
        SELECT COUNT(*) FROM crerule WHERE DOCURULE_FID = #{id} AND  STATUS != '1'
    </select>

</mapper>
