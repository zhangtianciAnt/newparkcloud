<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nt.service_AOCHUAN.AOCHUAN3000.mapper.ApplicationrecordMapper" >
    <insert id="insertApplicationrecordList" parameterType="java.util.List">
        INSERT INTO applicationrecord (applicationrecord_id, suppliername, supplierid, realpay, realdate, realamount, paiddate, invoiceno,paymentstatus, transportgood_id)
        values
        <foreach item="item" collection="list" separator=",">
            (#{item.applicationrecord_id}, #{item.suppliername}, #{item.supplierid}, #{item.realpay}, #{item.realdate}, #{item.realamount}, #{item.paiddate},
            #{item.invoiceno}, #{item.paymentstatus},#{item.transportgood_id})
        </foreach>
    </insert>
</mapper>
