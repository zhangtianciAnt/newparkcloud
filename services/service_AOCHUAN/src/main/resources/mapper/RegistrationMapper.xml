<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.nt.service_AOCHUAN.AOCHUAN3000.mapper.RegistrationMapper">

    <!--selectByRegId-->
    <select id="selectByRegId" resultType="com.nt.dao_AOCHUAN.AOCHUAN3000.Registration">
        SELECT *
        FROM REGISTRATION
        WHERE
        REG_ID = #{id}
        AND STATUS != '1'
    </select>
    <!--existCheck-->
    <select id="existCheck" resultType="int">
        SELECT COUNT(*)
        FROM REGISTRATION
        WHERE
        REG_ID = #{id}
        AND STATUS != '1'
    </select>
    <!--delete-->
    <update id="del">
        UPDATE REGISTRATION
		SET
		MODIFYON = now(),
		MODIFYBY = #{modifyby},
		STATUS = '1'
		WHERE REG_ID = #{id}
    </update>
</mapper>

